[package]
name = "trust"
version = "0.1.0"
edition = "2024"
description = "A retro DOS-style TUI IDE for Rust projects"
license = "MIT"

[dependencies]
crossterm = "0.29"
ratatui = "0.30"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
